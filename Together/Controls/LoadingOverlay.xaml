<UserControl x:Class="Together.Presentation.Controls.LoadingOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Visibility="{Binding IsLoading, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}">
    <Grid Background="#80000000">
        <!-- Semi-transparent overlay -->
        <Border Background="{DynamicResource MaterialDesignPaper}"
                CornerRadius="8"
                Padding="32"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                materialDesign:ElevationAssist.Elevation="Dp8">
            <StackPanel>
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             Width="64"
                             Height="64"
                             IsIndeterminate="True"
                             Foreground="{DynamicResource PrimaryHueMidBrush}"
                             Margin="0,0,0,16"/>
                <TextBlock Text="{Binding LoadingMessage, RelativeSource={RelativeSource AncestorType=UserControl}, FallbackValue='Please wait...'}"
                           Style="{StaticResource BodyTextStyle}"
                           TextAlignment="Center"
                           FontWeight="Medium"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
