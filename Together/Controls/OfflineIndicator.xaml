<UserControl x:Class="Together.Controls.OfflineIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    
    <Border Background="{DynamicResource MaterialDesignPaper}"
            BorderBrush="{DynamicResource MaterialDesignDivider}"
            BorderThickness="0,0,0,1"
            Padding="16,8"
            Visibility="{Binding IsOffline, Converter={StaticResource BooleanToVisibilityConverter}}">
        
        <StackPanel Orientation="Horizontal">
            <materialDesign:PackIcon Kind="CloudOffOutline"
                                    Width="20"
                                    Height="20"
                                    VerticalAlignment="Center"
                                    Foreground="{DynamicResource MaterialDesignBody}"
                                    Margin="0,0,8,0"/>
            
            <TextBlock Text="You are offline. Changes will be synced when connection is restored."
                      VerticalAlignment="Center"
                      Foreground="{DynamicResource MaterialDesignBody}"
                      FontSize="14"/>
            
            <!-- Pending operations count -->
            <Border Background="{DynamicResource PrimaryHueMidBrush}"
                    CornerRadius="10"
                    Padding="8,4"
                    Margin="12,0,0,0"
                    Visibility="{Binding PendingOperationCount, Converter={StaticResource ZeroToVisibilityConverter}}">
                <TextBlock Text="{Binding PendingOperationCount, StringFormat='{}{0} pending'}"
                          Foreground="White"
                          FontSize="12"
                          FontWeight="Medium"/>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
