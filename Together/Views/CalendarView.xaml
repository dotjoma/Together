<UserControl x:Class="Together.Presentation.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{DynamicResource MaterialDesignPaper}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with Days Together -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Event Calendar"
                           Style="{StaticResource MaterialDesignHeadline4TextBlock}"/>
                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <materialDesign:PackIcon Kind="Heart" 
                                            VerticalAlignment="Center"
                                            Foreground="{DynamicResource SecondaryHueMidBrush}"
                                            Width="20" Height="20"/>
                    <TextBlock Text="{Binding DaysTogether, StringFormat='{}{0} days together'}"
                               Style="{StaticResource MaterialDesignBody1TextBlock}"
                               Foreground="{DynamicResource SecondaryHueMidBrush}"
                               VerticalAlignment="Center"
                               Margin="5,0,0,0"/>
                </StackPanel>
            </StackPanel>

            <Button Grid.Column="1"
                    Content="Create Event"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding CreateEventCommand}"
                    VerticalAlignment="Center"/>
        </Grid>

        <!-- Calendar Navigation -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Style="{StaticResource MaterialDesignIconButton}"
                    Command="{Binding PreviousMonthCommand}"
                    ToolTip="Previous Month">
                <materialDesign:PackIcon Kind="ChevronLeft"/>
            </Button>

            <TextBlock Grid.Column="1"
                       Text="{Binding CurrentMonthDisplay}"
                       Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="2"
                    Style="{StaticResource MaterialDesignIconButton}"
                    Command="{Binding NextMonthCommand}"
                    ToolTip="Next Month">
                <materialDesign:PackIcon Kind="ChevronRight"/>
            </Button>

            <Button Grid.Column="3"
                    Content="Today"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Command="{Binding TodayCommand}"
                    Margin="10,0,0,0"/>
        </Grid>

        <!-- Upcoming Events -->
        <materialDesign:Card Grid.Row="2" Margin="0,0,0,15" Padding="15">
            <StackPanel>
                <TextBlock Text="Upcoming Events"
                           Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                           Margin="0,0,0,10"/>

                <ItemsControl ItemsSource="{Binding UpcomingEvents}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{DynamicResource MaterialDesignDivider}"
                                    BorderThickness="0,0,0,1"
                                    Padding="0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Margin="0,0,15,0">
                                        <TextBlock Text="{Binding EventDate, StringFormat='{}{0:MMM}'}"
                                                   Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                   HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding EventDate, StringFormat='{}{0:dd}'}"
                                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                                   HorizontalAlignment="Center"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Title}"
                                                   Style="{StaticResource MaterialDesignBody1TextBlock}"
                                                   FontWeight="Medium"/>
                                        <TextBlock Text="{Binding EventDate, StringFormat='{}{0:h:mm tt}'}"
                                                   Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                   Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                    </StackPanel>

                                    <materialDesign:PackIcon Grid.Column="2"
                                                            Kind="Bell"
                                                            VerticalAlignment="Center"
                                                            Foreground="{DynamicResource SecondaryHueMidBrush}"
                                                            Visibility="{Binding HasReminder, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Text="No upcoming events"
                           Style="{StaticResource MaterialDesignBody2TextBlock}"
                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                           HorizontalAlignment="Center"
                           Margin="0,10,0,0"
                           Visibility="{Binding UpcomingEvents.Count, Converter={StaticResource ZeroToVisibilityConverter}}"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- Month Events List -->
        <materialDesign:Card Grid.Row="3" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Events This Month"
                           Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                           Margin="0,0,0,10"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding MonthEvents}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{DynamicResource MaterialDesignDivider}"
                                        BorderThickness="0,0,0,1"
                                        Padding="0,10"
                                        Cursor="Hand">
                                    <Border.InputBindings>
                                        <MouseBinding MouseAction="LeftClick"
                                                     Command="{Binding DataContext.EditEventCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                     CommandParameter="{Binding}"/>
                                    </Border.InputBindings>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding Title}"
                                                       Style="{StaticResource MaterialDesignBody1TextBlock}"
                                                       FontWeight="Medium"/>
                                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                                <materialDesign:PackIcon Kind="Calendar" 
                                                                        Width="16" Height="16"
                                                                        VerticalAlignment="Center"
                                                                        Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                                <TextBlock Text="{Binding EventDate, StringFormat='{}{0:MMM dd, yyyy h:mm tt}'}"
                                                           Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                           Margin="5,0,0,0"/>
                                            </StackPanel>
                                            <TextBlock Text="{Binding Description}"
                                                       Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                       TextWrapping="Wrap"
                                                       Margin="0,5,0,0"
                                                       Visibility="{Binding Description, Converter={StaticResource StringToVisibilityConverter}}"/>
                                            <StackPanel Orientation="Horizontal" 
                                                       Margin="0,5,0,0"
                                                       Visibility="{Binding Recurrence, Converter={StaticResource RecurrenceVisibilityConverter}}">
                                                <materialDesign:PackIcon Kind="Repeat" 
                                                                        Width="16" Height="16"
                                                                        VerticalAlignment="Center"
                                                                        Foreground="{DynamicResource SecondaryHueMidBrush}"/>
                                                <TextBlock Text="{Binding Recurrence, StringFormat='Repeats {0}'}"
                                                           Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                           Foreground="{DynamicResource SecondaryHueMidBrush}"
                                                           Margin="5,0,0,0"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <Button Grid.Column="1"
                                                Style="{StaticResource MaterialDesignIconButton}"
                                                Command="{Binding DataContext.DeleteEventCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                ToolTip="Delete Event">
                                            <materialDesign:PackIcon Kind="Delete"/>
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <TextBlock Grid.Row="1"
                           Text="No events this month"
                           Style="{StaticResource MaterialDesignBody2TextBlock}"
                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="{Binding MonthEvents.Count, Converter={StaticResource ZeroToVisibilityConverter}}"/>
            </Grid>
        </materialDesign:Card>

        <!-- Loading Indicator -->
        <Grid Grid.Row="0" Grid.RowSpan="4"
              Background="#80000000"
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                         IsIndeterminate="True"
                         Width="50" Height="50"/>
        </Grid>
    </Grid>
</UserControl>
