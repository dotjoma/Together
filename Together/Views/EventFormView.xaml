<UserControl x:Class="Together.Presentation.Views.EventFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{DynamicResource MaterialDesignPaper}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   Text="{Binding IsEditMode, Converter={StaticResource EventFormTitleConverter}}"
                   Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                   Margin="0,0,0,20"/>

        <!-- Event Title -->
        <TextBox Grid.Row="1"
                 materialDesign:HintAssist.Hint="Event Title"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,15"/>

        <!-- Date and Time -->
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DatePicker Grid.Column="0"
                        materialDesign:HintAssist.Hint="Event Date"
                        Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                        SelectedDate="{Binding EventDate}"/>

            <materialDesign:TimePicker Grid.Column="2"
                                       materialDesign:HintAssist.Hint="Event Time"
                                       Style="{StaticResource MaterialDesignOutlinedTimePicker}"
                                       SelectedTime="{Binding EventTime}"
                                       Is24Hours="False"/>
        </Grid>

        <!-- Description -->
        <TextBox Grid.Row="3"
                 materialDesign:HintAssist.Hint="Description (Optional)"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 MinLines="3"
                 MaxLines="5"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,15"/>

        <!-- Recurrence -->
        <ComboBox Grid.Row="4"
                  materialDesign:HintAssist.Hint="Recurrence"
                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                  ItemsSource="{Binding RecurrenceOptions}"
                  SelectedItem="{Binding SelectedRecurrence}"
                  Margin="0,0,0,15"/>

        <!-- Error Message -->
        <TextBlock Grid.Row="5"
                   Text="{Binding ErrorMessage}"
                   Foreground="{DynamicResource MaterialDesignValidationErrorBrush}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="6" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="Cancel"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Command="{Binding CancelCommand}"
                    Margin="0,0,10,0"
                    Width="100"/>
            <Button Content="Save"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding SaveCommand}"
                    Width="100"/>
        </StackPanel>
    </Grid>
</UserControl>
