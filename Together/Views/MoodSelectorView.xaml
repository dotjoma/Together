<UserControl x:Class="Together.Presentation.Views.MoodSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   Text="How are you feeling today?"
                   Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                   Margin="0,0,0,20"/>

        <!-- Mood Selection -->
        <ListBox Grid.Row="1"
                 ItemsSource="{Binding MoodOptions}"
                 SelectedValue="{Binding SelectedMood}"
                 SelectedValuePath="Name"
                 Margin="0,0,0,20"
                 BorderThickness="0"
                 Background="Transparent">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="2"
                            BorderBrush="{Binding Color}"
                            CornerRadius="8"
                            Padding="15"
                            Margin="5"
                            Background="Transparent"
                            Cursor="Hand">
                        <StackPanel>
                            <TextBlock Text="{Binding Emoji}"
                                     FontSize="40"
                                     HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding Name}"
                                     FontSize="12"
                                     HorizontalAlignment="Center"
                                     Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#E3F2FD"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <!-- Notes Input -->
        <TextBox Grid.Row="2"
                 Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                 materialDesign:HintAssist.Hint="Add notes (optional)"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 MinHeight="80"
                 MaxHeight="150"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,20"/>

        <!-- Save Button -->
        <Button Grid.Row="3"
                Command="{Binding SaveMoodCommand}"
                Style="{StaticResource MaterialDesignRaisedButton}"
                HorizontalAlignment="Center"
                Width="200"
                IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="EmoticonHappy" 
                                       VerticalAlignment="Center" 
                                       Margin="0,0,8,0"
                                       Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             Width="16"
                             Height="16"
                             Margin="0,0,8,0"
                             IsIndeterminate="True"
                             Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <TextBlock Text="Save Mood" VerticalAlignment="Center"/>
            </StackPanel>
        </Button>
    </Grid>
</UserControl>
