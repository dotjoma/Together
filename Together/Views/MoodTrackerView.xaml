<UserControl x:Class="Together.Presentation.Views.MoodTrackerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:views="clr-namespace:Together.Presentation.Views"
             Background="#F5F5F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tab Navigation -->
        <materialDesign:Card Grid.Row="0" Margin="20,20,20,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <ToggleButton Content="Log Mood"
                            IsChecked="{Binding ShowSelector}"
                            Command="{Binding ShowSelectorCommand}"
                            Style="{StaticResource MaterialDesignFlatPrimaryToggleButton}"
                            Margin="5"
                            Padding="20,10"/>
                <ToggleButton Content="View History"
                            IsChecked="{Binding ShowHistory}"
                            Command="{Binding ShowHistoryCommand}"
                            Style="{StaticResource MaterialDesignFlatPrimaryToggleButton}"
                            Margin="5"
                            Padding="20,10"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- Content Area -->
        <materialDesign:Card Grid.Row="1" Margin="20,10,20,20">
            <Grid>
                <!-- Mood Selector -->
                <views:MoodSelectorView DataContext="{Binding MoodSelector}"
                                       Visibility="{Binding ShowSelector, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <!-- Mood History -->
                <views:MoodHistoryView DataContext="{Binding MoodHistory}"
                                      Visibility="{Binding ShowHistory, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
