<UserControl x:Class="Together.Presentation.Views.TodoListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:Together.Presentation.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:InverseBooleanConverter x:Key="InverseBool"/>
        <converters:BoolToStrikethroughConverter x:Key="BoolToStrikethrough"/>
        <converters:BoolToErrorBrushConverter x:Key="BoolToErrorBrush"/>
        <converters:StringToVisibilityConverter x:Key="StringToVis"/>
        <converters:ZeroToVisibilityConverter x:Key="ZeroToVis"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                      Text="Shared To-Do List"
                      Style="{StaticResource MaterialDesignHeadline4TextBlock}"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Add Task"
                       Command="{Binding CreateCommand}"
                       Style="{StaticResource MaterialDesignRaisedButton}"
                       IsEnabled="{Binding IsCreating, Converter={StaticResource InverseBool}}"
                       Margin="0,0,10,0"/>
                
                <Button Content="Refresh"
                       Command="{Binding RefreshCommand}"
                       Style="{StaticResource MaterialDesignOutlinedButton}"/>
            </StackPanel>
        </Grid>

        <!-- Filter Section -->
        <materialDesign:Card Grid.Row="1" Margin="0,0,0,10" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                        Text="{Binding FilterTags, UpdateSourceTrigger=PropertyChanged}"
                        materialDesign:HintAssist.Hint="Filter by tags (comma-separated)"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Margin="0,0,10,0"/>

                <CheckBox Grid.Column="1"
                         Content="Overdue Only"
                         IsChecked="{Binding ShowOverdueOnly}"
                         Margin="0,0,10,0"
                         VerticalAlignment="Center"/>

                <Button Grid.Column="2"
                       Content="Apply"
                       Command="{Binding ApplyFilterCommand}"
                       Style="{StaticResource MaterialDesignOutlinedButton}"
                       Margin="0,0,5,0"/>

                <Button Grid.Column="3"
                       Content="Clear"
                       Command="{Binding ClearFilterCommand}"
                       Style="{StaticResource MaterialDesignOutlinedButton}"/>
            </Grid>
        </materialDesign:Card>

        <!-- Create New Todo Form -->
        <materialDesign:Card Grid.Row="2" 
                            Margin="0,0,0,10" 
                            Padding="20"
                            Visibility="{Binding IsCreating, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <TextBlock Text="Create New Task"
                          Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                          Margin="0,0,0,15"/>

                <TextBox Text="{Binding NewTitle, UpdateSourceTrigger=PropertyChanged}"
                        materialDesign:HintAssist.Hint="Title *"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Margin="0,0,0,10"/>

                <TextBox Text="{Binding NewDescription, UpdateSourceTrigger=PropertyChanged}"
                        materialDesign:HintAssist.Hint="Description"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        AcceptsReturn="True"
                        TextWrapping="Wrap"
                        MinLines="2"
                        MaxLines="4"
                        Margin="0,0,0,10"/>

                <DatePicker SelectedDate="{Binding NewDueDate}"
                           materialDesign:HintAssist.Hint="Due Date"
                           Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                           Margin="0,0,0,10"/>

                <ComboBox materialDesign:HintAssist.Hint="Assign To"
                         SelectedValuePath="Tag"
                         SelectedValue="{Binding NewAssignedTo}"
                         Style="{StaticResource MaterialDesignOutlinedComboBox}"
                         Margin="0,0,0,10">
                    <ComboBoxItem Content="Unassigned">
                        <ComboBoxItem.Tag>
                            <x:Null/>
                        </ComboBoxItem.Tag>
                    </ComboBoxItem>
                    <ComboBoxItem Content="Me" Tag="{Binding DataContext.CurrentUserId, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    <ComboBoxItem Content="Partner" Tag="{Binding DataContext.PartnerId, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                </ComboBox>

                <TextBox Text="{Binding NewTags, UpdateSourceTrigger=PropertyChanged}"
                        materialDesign:HintAssist.Hint="Tags (comma-separated)"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Margin="0,0,0,15"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel"
                           Command="{Binding CancelNewCommand}"
                           Style="{StaticResource MaterialDesignOutlinedButton}"
                           Margin="0,0,10,0"/>
                    
                    <Button Content="Save"
                           Command="{Binding SaveNewCommand}"
                           Style="{StaticResource MaterialDesignRaisedButton}"/>
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>

        <!-- Todo Items List -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding TodoItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Margin="0,0,0,10" Padding="15">
                            <Grid>
                                <!-- Display Mode -->
                                <Grid>
                                    <Grid.Style>
                                        <Style TargetType="Grid">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Grid.Style>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Checkbox -->
                                    <CheckBox Grid.Column="0"
                                             IsChecked="{Binding Completed}"
                                             Command="{Binding ToggleCompleteCommand}"
                                             VerticalAlignment="Top"
                                             Margin="0,5,15,0"/>

                                    <!-- Content -->
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Title}"
                                                  Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                                  TextDecorations="{Binding Completed, Converter={StaticResource BoolToStrikethrough}}"/>

                                        <TextBlock Text="{Binding Description}"
                                                  Style="{StaticResource MaterialDesignBody2TextBlock}"
                                                  TextWrapping="Wrap"
                                                  Margin="0,5,0,0"
                                                  Visibility="{Binding Description, Converter={StaticResource StringToVis}}"/>

                                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                            <!-- Due Date -->
                                            <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                                                <materialDesign:PackIcon Kind="Calendar" 
                                                                        VerticalAlignment="Center"
                                                                        Margin="0,0,5,0"
                                                                        Foreground="{Binding IsOverdue, Converter={StaticResource BoolToErrorBrush}}"/>
                                                <TextBlock Text="{Binding DisplayDueDate}"
                                                          VerticalAlignment="Center"
                                                          Foreground="{Binding IsOverdue, Converter={StaticResource BoolToErrorBrush}}"/>
                                            </StackPanel>

                                            <!-- Assignment -->
                                            <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                                                <materialDesign:PackIcon Kind="Account" 
                                                                        VerticalAlignment="Center"
                                                                        Margin="0,0,5,0"/>
                                                <TextBlock Text="{Binding DisplayAssignment}"
                                                          VerticalAlignment="Center"/>
                                            </StackPanel>

                                            <!-- Tags -->
                                            <StackPanel Orientation="Horizontal"
                                                       Visibility="{Binding Tags.Count, Converter={StaticResource ZeroToVis}, ConverterParameter=Inverse}">
                                                <materialDesign:PackIcon Kind="Tag" 
                                                                        VerticalAlignment="Center"
                                                                        Margin="0,0,5,0"/>
                                                <TextBlock Text="{Binding DisplayTags}"
                                                          VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>

                                    <!-- Actions -->
                                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                                        <Button Command="{Binding EditCommand}"
                                               Style="{StaticResource MaterialDesignIconButton}"
                                               ToolTip="Edit">
                                            <materialDesign:PackIcon Kind="Pencil"/>
                                        </Button>
                                        
                                        <Button Command="{Binding DeleteCommand}"
                                               Style="{StaticResource MaterialDesignIconButton}"
                                               ToolTip="Delete">
                                            <materialDesign:PackIcon Kind="Delete"/>
                                        </Button>
                                    </StackPanel>
                                </Grid>

                                <!-- Edit Mode -->
                                <StackPanel Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}">
                                    <TextBox Text="{Binding EditTitle, UpdateSourceTrigger=PropertyChanged}"
                                            materialDesign:HintAssist.Hint="Title *"
                                            Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                            Margin="0,0,0,10"/>

                                    <TextBox Text="{Binding EditDescription, UpdateSourceTrigger=PropertyChanged}"
                                            materialDesign:HintAssist.Hint="Description"
                                            Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                            AcceptsReturn="True"
                                            TextWrapping="Wrap"
                                            MinLines="2"
                                            MaxLines="4"
                                            Margin="0,0,0,10"/>

                                    <DatePicker SelectedDate="{Binding EditDueDate}"
                                               materialDesign:HintAssist.Hint="Due Date"
                                               Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                                               Margin="0,0,0,10"/>

                                    <TextBox Text="{Binding EditTags, UpdateSourceTrigger=PropertyChanged}"
                                            materialDesign:HintAssist.Hint="Tags (comma-separated)"
                                            Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                            Margin="0,0,0,15"/>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Button Content="Cancel"
                                               Command="{Binding CancelCommand}"
                                               Style="{StaticResource MaterialDesignOutlinedButton}"
                                               Margin="0,0,10,0"/>
                                        
                                        <Button Content="Save"
                                               Command="{Binding SaveCommand}"
                                               Style="{StaticResource MaterialDesignRaisedButton}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Loading Indicator -->
        <Grid Grid.Row="3" 
              Background="#80000000"
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                        Value="0"
                        IsIndeterminate="True"
                        Width="50"
                        Height="50"/>
        </Grid>
    </Grid>
</UserControl>
