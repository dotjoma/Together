<UserControl x:Class="Together.Presentation.Views.UpdateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Application Updates" 
                   Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                   Margin="0,0,0,20"/>

        <!-- Version Information -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Current Version:" 
                       Style="{StaticResource MaterialDesignBody2TextBlock}"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            <TextBlock Text="{Binding CurrentVersion}" 
                       Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                       Margin="0,5,0,10"/>

            <TextBlock Text="Available Version:" 
                       Style="{StaticResource MaterialDesignBody2TextBlock}"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"
                       Visibility="{Binding UpdateAvailable, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            <TextBlock Text="{Binding AvailableVersion}" 
                       Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                       Margin="0,5,0,0"
                       Visibility="{Binding UpdateAvailable, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>

        <!-- Update Status Message -->
        <Border Grid.Row="2" 
                Background="{DynamicResource MaterialDesignPaper}"
                CornerRadius="4"
                Padding="15"
                Margin="0,0,0,20"
                Visibility="{Binding UpdateMessage, Converter={StaticResource StringToVisibilityConverter}}">
            <StackPanel>
                <TextBlock Text="{Binding UpdateMessage}" 
                           TextWrapping="Wrap"
                           Style="{StaticResource MaterialDesignBody1TextBlock}"/>

                <!-- Required Update Warning -->
                <Border Background="{DynamicResource SecondaryHueMidBrush}"
                        CornerRadius="4"
                        Padding="10"
                        Margin="0,10,0,0"
                        Visibility="{Binding IsUpdateRequired, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AlertCircle" 
                                               Width="20" 
                                               Height="20"
                                               VerticalAlignment="Center"
                                               Margin="0,0,10,0"/>
                        <TextBlock Text="This is a required update and must be installed." 
                                   TextWrapping="Wrap"
                                   VerticalAlignment="Center"
                                   Foreground="White"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <!-- Download Progress -->
        <StackPanel Grid.Row="3" 
                    Margin="0,0,0,20"
                    Visibility="{Binding IsInstallingUpdate, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="Download Progress:" 
                       Style="{StaticResource MaterialDesignBody2TextBlock}"
                       Margin="0,0,0,5"/>
            <ProgressBar Value="{Binding DownloadProgress}" 
                         Minimum="0" 
                         Maximum="100"
                         Height="8"
                         Style="{StaticResource MaterialDesignLinearProgressBar}"/>
            <TextBlock Text="{Binding DownloadProgress, StringFormat={}{0}%}" 
                       HorizontalAlignment="Center"
                       Margin="0,5,0,0"
                       Style="{StaticResource MaterialDesignBody2TextBlock}"/>
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom">
            <Button Content="Check for Updates"
                    Command="{Binding CheckForUpdatesCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,10,0"
                    IsEnabled="{Binding IsNetworkDeployed}">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Refresh" 
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,8,0"/>
                            <TextBlock Text="Check for Updates" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>

            <Button Content="Install Update"
                    Command="{Binding InstallUpdateCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Visibility="{Binding UpdateAvailable, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Download" 
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,8,0"/>
                            <TextBlock Text="Install Update" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
        </StackPanel>

        <!-- Not Network Deployed Message -->
        <Border Grid.Row="4"
                Background="{DynamicResource MaterialDesignPaper}"
                CornerRadius="4"
                Padding="15"
                VerticalAlignment="Center"
                Visibility="{Binding IsNetworkDeployed, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center">
                <materialDesign:PackIcon Kind="InformationOutline" 
                                       Width="48" 
                                       Height="48"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,10"
                                       Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                <TextBlock Text="Automatic updates are not available" 
                           Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,5"/>
                <TextBlock Text="This application is not deployed via ClickOnce." 
                           Style="{StaticResource MaterialDesignBody2TextBlock}"
                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.Row="0" 
                Grid.RowSpan="5"
                Background="#80000000"
                Visibility="{Binding IsCheckingForUpdates, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             Value="0"
                             IsIndeterminate="True"
                             Width="48"
                             Height="48"/>
                <TextBlock Text="Checking for updates..." 
                           Foreground="White"
                           Margin="0,10,0,0"
                           Style="{StaticResource MaterialDesignBody1TextBlock}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
